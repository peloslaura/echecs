#ifndef CAVALIER_HPP
#define CAVALIER_HPP

#include "piece.hpp"
#include "square.hpp"
#include <string>

class Cavalier : public Piece {
public:
    Cavalier(std::string couleur, std::string nom, int id, Square position);
    virtual ~Cavalier();
    bool est_mouvement_legal(Square case_origine, Square case_destination) const override;
};

#endif